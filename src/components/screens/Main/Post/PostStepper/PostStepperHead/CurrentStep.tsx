import { Box } from "@mui/material";

const CurrentStep = () => {
  return (
    <Box
      sx={{
        width: "17px",
        height: "3px",
        borderRadius: "1px",
        backgroundColor: "primary.main",
      }}
    />
  );
};

export default CurrentStep;
